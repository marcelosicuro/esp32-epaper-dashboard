substitutions:
  device_name: teste-minimo

esphome:
  name: ${device_name}

esp32:
  board: esp32-c3-devkitm-1
  framework:
    type: arduino

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "Teste Setup"
    password: "teste1234"

captive_portal:
logger:

ota:
  - platform: esphome
    password: !secret ota_password

web_server:
  port: 80
